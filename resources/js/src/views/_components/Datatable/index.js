import React from 'react';
import useDatatable from './useDatatable';

const datatable = props => {
    const {modal, openModal} = useDatatable();

    return(
        <div>
            
        </div>
    )
}

export default datatable;